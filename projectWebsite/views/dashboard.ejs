<!DOCTYPE html>
<html lang="th" data-bs-theme="dark">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard - BWS Esports Bot</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css"> </head>
<body>
    <nav class="navbar navbar-expand-lg bg-body-tertiary">
        <div class="container-fluid">
            <a class="navbar-brand" href="#">BWS Bot Dashboard</a>
            <div class="collapse navbar-collapse">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    <li class="nav-item"><a class="nav-link active" href="#">General</a></li>
                    <li class="nav-item"><a class="nav-link" href="#">Ticket Panel</a></li>
                    <li class="nav-item"><a class="nav-link" href="#">Moderation</a></li>
                </ul>
                <div class="d-flex align-items-center">
                    <img src="https://cdn.discordapp.com/avatars/<%= user.id %>/<%= user.avatar %>.png" width="32" height="32" class="rounded-circle me-2">
                    <span class="navbar-text me-3">Welcome, <%= user.username %></span>
                    <a href="/logout" class="btn btn-danger btn-sm">Logout</a>
                </div>
            </div>
        </div>
    </nav>

    <main class="container mt-4">
        <h2>General Settings</h2>
        <p>ตั้งค่า ID และค่าพื้นฐานต่างๆ ของบอท</p>
        <hr>

        <form action="/dashboard/save" method="POST">
            <div class="mb-3">
                <label for="staffRoleId" class="form-label">STAFF_ROLE_ID</label>
                <input type="text" class="form-control" id="staffRoleId" name="staffRoleId" value="<%= settings.staffRoleId || '' %>" placeholder="ใส่ ID ของยศทีมงาน">
            </div>
            <div class="mb-3">
                <label for="ticketCategoryId" class="form-label">TICKET_CATEGORY_ID</label>
                <input type="text" class="form-control" id="ticketCategoryId" name="ticketCategoryId" value="<%= settings.ticketCategoryId || '' %>" placeholder="ใส่ ID ของหมวดหมู่ที่จะสร้าง Ticket">
            </div>
            <div class="mb-3">
                <label for="logChannelId" class="form-label">LOG_CHANNEL_ID</label>
                <input type="text" class="form-control" id="logChannelId" name="logChannelId" value="<%= settings.logChannelId || '' %>" placeholder="ใส่ ID ของห้องสำหรับเก็บ Log">
            </div>
            
            <button type="submit" class="btn btn-primary">💾 Save Settings</button>
        </form>
    </main>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>